-Added support for new wallet v1.3.9
-Fixed a bug which caused building from source to fail
-Fixed a bug which caused the downloaded archive directory to be extracted but not copied properly
-Building from source now does a git checkout of the latest version instead of always building from the master source